--extra-index-url https://download.pytorch.org/whl/cpu

# The CPU build. The default CUDA wheel pulls ~2.7 GB of nvidia libraries that a
# free CPU Space can never use.
torch==2.12.0
numpy==2.4.6
pillow==12.2.0
fastapi==0.141.1
uvicorn==0.52.3
python-multipart
