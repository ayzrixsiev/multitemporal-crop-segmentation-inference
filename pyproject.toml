[project]
name = "utae-crop-temporal-segmentation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "geopandas>=1.1.3",
    "numpy>=2.4.6",
    "pandas>=3.0.3",
    "torch>=2.12.0",
    # webapp/ — the viewer. FastAPI serves the page and the JSON API, uvicorn
    # runs it, python-multipart is what lets it accept an uploaded file, and
    # Pillow encodes the 128x128 maps as PNGs.
    "fastapi>=0.115",
    "uvicorn>=0.34",
    "python-multipart>=0.0.20",
    "pillow>=11.0",
]
